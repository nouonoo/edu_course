# Приложение Н. Диаграмма компонентов и развёртывания (UML)

**Проект:** Адаптационный курс для сотрудников ритуальной компании  
**Нотация:** UML Component / Deployment Diagram  
**Версия:** 1.0  
**Дата:** июнь 2026

---

## Рисунок Н-1. Диаграмма компонентов

```mermaid
flowchart TB
    subgraph Client["Клиент (браузер)"]
        FE["Компонент Frontend\nHTML/CSS/JS\nпорт 8000"]
        Player["Компонент Course Player\nbezopasnost / SCORM"]
    end

    subgraph Server["Сервер приложений"]
        API["Компонент Flask API\napp.py"]
        DBMod["Компонент Database\ndatabase.py"]
        Prog["Компонент Progress\ncourse_progress.py"]
        Man["Компонент Manifest\ncourse_manifest.py"]
        Scorm["Компонент SCORM\nscorm_*.py"]
        Files["Компонент File Storage\ncourse-content, uploads"]
    end

    subgraph Data["Уровень данных"]
        SQL[("SQL Server\nLearningPlatformDB")]
        Disk[("Файловое хранилище\nbackend/courses/")]
    end

    FE -->|REST /api/*| API
    FE -->|НАЧАТЬ КУРС| Files
    Player -->|JWT в query| API
    API --> DBMod
    API --> Prog
    API --> Man
    API --> Scorm
    API --> Files
    DBMod -->|ODBC pyodbc| SQL
    Files --> Disk
    Man --> Disk
```

---

## Рисунок Н-2. Диаграмма развёртывания

```mermaid
flowchart LR
    subgraph Workstation["Рабочая станция пользователя"]
        Browser["Браузер\nChrome / Edge"]
    end

    subgraph AppServer["Локальный сервер разработки"]
        HTTP8000["Python http.server\n:8000 frontend/"]
        Flask5000["Flask app.py\n:5000 backend/"]
    end

    subgraph DBServer["Сервер БД"]
        MSSQL["SQL Server\nLearningPlatformDB"]
    end

    Browser -->|HTTP| HTTP8000
    Browser -->|fetch API| Flask5000
    Browser -->|course-content| Flask5000
    Flask5000 -->|ODBC 17| MSSQL
    Flask5000 -->|read/write| FS["Локальный диск\ncourses/, uploads/"]
```

---

## Таблица компонентов

| Компонент | Файлы | Интерфейс | Зависимости |
|-----------|-------|-----------|-------------|
| Frontend | `frontend/*.html`, `*.js` | HTTP к :5000/api | — |
| Flask API | `app.py` | REST JSON, binary xlsx | Database, JWT |
| Database | `database.py` | Python methods | pyodbc, SQL Server |
| Course Player | `bezopasnost/*` | LMSBridge → REST | Flask API |
| SCORM Runtime | `scorm/*`, `scorm_runtime.py` | SCORM 2004 API | JSON state files |
| File Storage | `courses/`, `uploads/` | send_file | OS filesystem |

---

## Порты и протоколы

| Сервис | Порт | Протокол | Назначение |
|--------|------|----------|------------|
| Frontend | 8000 | HTTP | Статика LMS |
| Backend API | 5000 | HTTP/REST | JSON, JWT, файлы курсов |
| SQL Server | 1433* | TDS/ODBC | База данных |

\* Порт зависит от конфигурации экземпляра SQL Server.

---

## Примечание для переноса в Word

Вставьте как **«Приложение Н. Диаграмма компонентов и развёртывания»**.
